node_modules/
.next/
bin/
obj/
*.db
*.db-shm
*.db-wal
.env.local

# Raw 3D asset exports (96MB source zip + unpacked GLBs) — only the
# compressed, web-ready output in frontend/public/models/ is committed.
.scratch-3d/
*.zip


# Local backup of files moved out during the UX/SEO audit (originals, retired components).
_respaldo-auditoria/

# Images uploaded from the admin panel at runtime (user content, not source).
backend/LAmour.Api/wwwroot/uploads/

# Firebase service-account keys (server secrets) — never commit.
*firebase-adminsdk*.json
firebase-admin*.json
service-account*.json
